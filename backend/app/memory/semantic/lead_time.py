"""Lead-time statistics from episodic expansion events (Phase B scaffold)."""

from __future__ import annotations


def median_lead_time_hours(signal: str, *, events: list[dict] | None = None) -> float | None:
    """Median hours from signal to expansion once semantic consolidation runs."""
    del signal, events
    return None
